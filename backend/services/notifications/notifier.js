export class Notifier {
  /**
   * Send a notification
   * @param {NotificationDTO} notificationDTO - The notification data transfer object
   * @returns {Promise<void>}
   */
  async send(notificationDTO) {
  }
}
