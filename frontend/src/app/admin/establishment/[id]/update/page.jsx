'use client';

import Layout from "@/app/components/base_layout/Layout";
import React from "react";

export default function updateEstablishment({params}){
  const {id} = React.use(params);

  return (
    <Layout>

    </Layout>
  )
}